import os
from openai import AsyncOpenAI

client = AsyncOpenAI(api_key=os.getenv("OPENAI_API_KEY"))

# 🔍 Быстрые ответы по ключевым словам
FAQ = {
    "обременени": "🔐 Да, на объекте есть обременение (кредит). Оно будет закрыто полностью до выхода на сделку.",
    "аренд": "🏢 Арендатор — ООО «Ваш Дом». Объект передан ФСИН в безвозмездное пользование. Это обеспечивает загрузку и социальную устойчивость.",
    "фсин": (
        "👥 Объект используется ФСИН (женский исправительный центр) круглосуточно (режим 24/7). "
        "На территории работает постоянный штат сотрудников, установлено видеонаблюдение, здание полностью соответствует "
        "требованиям *СП 308.13330.2012* (здания исправительных учреждений).\n\n"
        "Исправительные работы выполняются осуждёнными по административным и хозяйственным направлениям. "
        "Соглашение с ФСИН — безвозмездное, но обеспечивает надёжную загрузку, охрану и социальную стабильность объекта."
    ),
}

# 🧾 Характеристики объекта (вшиваются в каждый GPT-запрос)
SUMMARY = """
📍 *Адрес*: Калуга, пер. Сельский, 8а  
🏢 *Объект*: Гостиница, переоборудованная под исправительный центр  
📐 *Площадь*: 1089,7 м² (здание) + 815 м² (земля)  
💰 *Цена*: 56 млн ₽  
👥 *Используется ФСИН (УФИЦ)* — по безвозмездному соглашению  
🏛️ *Арендатор*: ООО «Ваш Дом» (собственник)  
🔒 *Обременение*: есть, будет погашено до сделки  
📋 *Документы*: техплан, выписки ЕГРН, резюме, презентация, Свод правил 308
"""

# 🧠 GPT-фильтр и стиль общения
FILTER_PROMPT = """
Ты — эксперт по продаже доходной недвижимости. Общайся делово, структурированно и без лишней воды.

Если вопрос размытый — уточни:
> Уточните, вы представляете интересы клиента или просто изучаете объект?

Если вопрос по делу — ответь с акцентом на преимущества:
- ФСИН работает 24/7, обеспечивает порядок и охрану
- Здание полностью оборудовано и соответствует СП 308.13330.2012
- Собственник готов закрыть обременение
- Документы готовы, собственность оформлена

Ответ делай в стиле Telegram:
1. ✳️ Краткий вывод  
2. 💼 Преимущество для инвестора  
3. 📎 Уточнение или CTA (предложение связаться)
"""

# 🤖 Главная функция
async def get_answer(question: str) -> str:
    q_lower = question.lower()

    for keyword, reply in FAQ.items():
        if keyword in q_lower:
            return reply

    prompt = f"{SUMMARY}\n\n{FILTER_PROMPT}\n\nВопрос клиента: \"{question}\"\n\nОтвет:"

    try:
        response = await client.chat.completions.create(
            model="gpt-4",
            messages=[{"role": "user", "content": prompt}]
        )
        return response.choices[0].message.content.strip()
    except Exception:
        return "🏢 Объект в собственности. Документы готовы. Уточните, вы представляете клиента?"