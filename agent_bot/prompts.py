from openai import AsyncOpenAI
import os

client = AsyncOpenAI(api_key=os.getenv("OPENAI_API_KEY"))

# Быстрые ответы по ключевым словам
FAQ = {
    "обременени": "Обременений нет. Объект в собственности ООО «Ваш Дом».",
    "аренд": "Мы готовы остаться арендаторами на 3 года по ставке 120 000 ₽/мес. Это обеспечит стабильный доход.",
}

# Характеристики (вшить после OCR)
SUMMARY = """
📍 Адрес: Калуга, пер. Сельский, 8а  
🏢 Площадь: 1089,7 м² + 815 м² (земля)  
💰 Цена: 74 млн ₽  
📃 Документы: техплан, ЕГРН на здание и землю, резюме объекта, Свод правил 308  
🔐 Обременения: нет  
📦 Потенциал: сдача в аренду/реконструкция/готовый гостиничный бизнес  
"""

FILTER_PROMPT = """
Твоя задача — отсеивать случайных интересантов, перекупщиков без клиентов и тех, кто пишет "просто посмотреть".  
Работай **только с теми**, кто:
- имеет контакт с инвестором или клиентом,
- задаёт конкретные вопросы по объекту,
- сам занимается коммерцией или брокер.

Если человек подходит — **давай сильные доводы для покупки**: локация, готовность, документы, арендаторы.
"""

async def get_answer(question: str) -> str:
    for k, v in FAQ.items():
        if k in question.lower():
            return v

    prompt = f"""
{SUMMARY}

{FILTER_PROMPT}

Вопрос клиента: {question}
Ответь как уверенный агент, с фильтром на целевого клиента. Коротко и по делу.
"""
    try:
        response = await client.chat.completions.create(
            model="gpt-4",
            messages=[{"role": "user", "content": prompt}]
        )
        return response.choices[0].message.content.strip()
    except Exception:
        return "🏢 Объект в собственности. Документы готовы. Уточните, вы представляете клиента?"
